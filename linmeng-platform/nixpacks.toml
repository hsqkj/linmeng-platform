[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = ['cd server && npm install']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'cd server && npx sequelize-cli db:migrate && node src/app.js'
